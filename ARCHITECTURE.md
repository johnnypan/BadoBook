# 羽约吧 - 系统架构文档

## 📐 系统架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                        前端应用层                              │
│                    (React + TypeScript)                      │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ 首页视图  │  │ 预订管理  │  │ 组队匹配  │  │ 个人中心  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│         │              │              │              │      │
│         └──────────────┴──────────────┴──────────────┘      │
│                          │                                  │
│                    ┌─────▼─────┐                           │
│                    │  API 层    │                           │
│                    └─────┬─────┘                           │
└──────────────────────────┼──────────────────────────────────┘
                           │
                    ┌──────▼──────┐
                    │ Supabase SDK │
                    └──────┬──────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    Supabase 后端服务                          │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │PostgreSQL│  │   Auth   │  │ Realtime │  │ Storage  │   │
│  │  数据库   │  │  认证服务 │  │  实时订阅 │  │ 文件存储  │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 🗂️ 数据库架构

### ER 图（实体关系图）

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   profiles  │         │   venues    │         │  bookings   │
├─────────────┤         ├─────────────┤         ├─────────────┤
│ id (PK)     │         │ id (PK)     │         │ id (PK)     │
│ username    │         │ name        │    ┌────│ user_id(FK) │
│ avatar_url  │         │ rating      │    │    │ venue_id(FK)│
│ skill_level │         │ price       │    │    │ status      │
│ ...         │         │ ...         │    │    │ ...         │
└──────┬──────┘         └──────┬──────┘    │    └──────┬──────┘
       │                       │           │           │
       │                       │           │           │
       │                       └───────────┘           │
       │                                               │
       │                                               │
       │         ┌─────────────────────┐              │
       │         │ booking_participants │              │
       │         ├─────────────────────┤              │
       │         │ id (PK)             │              │
       └─────────│ user_id (FK)        │              │
                 │ booking_id (FK)     │──────────────┘
                 │ is_organizer        │
                 └─────────────────────┘

┌─────────────────┐         ┌──────────────────┐
│team_invitations │         │ team_participants │
├─────────────────┤         ├──────────────────┤
│ id (PK)         │────────│ invitation_id(FK)│
│ creator_id (FK) │         │ user_id (FK)     │
│ venue_id (FK)   │         │ status           │
│ skill_level     │         └──────────────────┘
│ missing_count   │
│ status          │
└─────────────────┘
```

### 核心表说明

#### 1. profiles (用户资料)
- 存储用户基本信息
- 与 Supabase Auth 关联
- 技能等级、头像等扩展信息

#### 2. venues (场馆)
- 场馆基本信息
- 价格、评分、设施
- 地理位置信息

#### 3. bookings (预订)
- 预订记录
- 关联用户和场馆
- 状态管理（待使用/已完成/已取消）

#### 4. team_invitations (组队邀约)
- 组队信息
- 技能要求
- 缺人数量

#### 5. notifications (通知)
- 系统通知
- 实时推送支持

## 🔄 数据流图

### 用户预订流程

```
用户操作                API 调用                数据库操作
   │                      │                        │
   ├─ 选择场馆             │                        │
   │                      │                        │
   ├─ 填写预订信息         │                        │
   │                      │                        │
   ├─ 提交预订 ──────────▶ createBooking()         │
   │                      │                        │
   │                      ├─ 验证用户登录           │
   │                      │                        │
   │                      ├─ 插入预订记录 ────────▶ INSERT bookings
   │                      │                        │
   │                      ├─ 添加参与者 ──────────▶ INSERT booking_participants
   │                      │                        │
   │                      ├─ 创建通知 ────────────▶ INSERT notifications
   │                      │                        │
   │                      ├─ 返回预订信息           │
   │ ◀──────────────────── │                        │
   │                      │                        │
   ├─ 显示预订成功         │                        │
```

### 组队匹配流程

```
发起人                  申请人                  数据库
   │                      │                        │
   ├─ 创建邀约             │                        │
   │ ────────────────────▶ INSERT team_invitations │
   │                      │                        │
   │                      ├─ 浏览邀约               │
   │                      │ ◀───────────────────── SELECT
   │                      │                        │
   │                      ├─ 申请加入               │
   │                      │ ────────────────────▶ INSERT team_participants
   │                      │                        │
   │ ◀─────────────────── │ ────────────────────▶ INSERT notifications
   │  (收到通知)           │                        │
   │                      │                        │
   ├─ 审批申请             │                        │
   │ ────────────────────▶ UPDATE team_participants│
   │                      │                        │
   │                      │ ◀───────────────────── UPDATE + NOTIFY
   │                      │  (收到通知)             │
```

## 🔐 安全架构

### Row Level Security (RLS) 策略

```sql
-- 用户只能查看自己的预订
CREATE POLICY "Users can view own bookings" 
ON bookings FOR SELECT 
USING (auth.uid() = user_id);

-- 用户只能创建自己的预订
CREATE POLICY "Users can create own bookings" 
ON bookings FOR INSERT 
WITH CHECK (auth.uid() = user_id);

-- 所有人可以查看开放的组队邀约
CREATE POLICY "Open invitations viewable by all" 
ON team_invitations FOR SELECT 
USING (status = 'open' OR auth.uid() = creator_id);
```

### 认证流程

```
客户端                  Supabase Auth           数据库
   │                      │                        │
   ├─ 注册/登录请求        │                        │
   │ ────────────────────▶ 验证凭证                │
   │                      │                        │
   │                      ├─ 生成 JWT Token        │
   │                      │                        │
   │                      ├─ 触发 trigger ────────▶ 创建 profile
   │                      │                        │
   │ ◀──────────────────── 返回 Token + Session    │
   │                      │                        │
   │                      │                        │
   ├─ API 请求 (带 Token) │                        │
   │ ────────────────────▶ 验证 Token              │
   │                      │                        │
   │                      ├─ 检查 RLS 策略 ───────▶ 执行查询
   │                      │                        │
   │ ◀──────────────────── 返回数据                │
```

## 📊 性能优化

### 数据库索引

```sql
-- 预订查询优化
CREATE INDEX idx_bookings_user_id ON bookings(user_id);
CREATE INDEX idx_bookings_date ON bookings(booking_date);
CREATE INDEX idx_bookings_status ON bookings(status);

-- 组队查询优化
CREATE INDEX idx_team_invitations_date ON team_invitations(game_date);
CREATE INDEX idx_team_invitations_status ON team_invitations(status);

-- 通知查询优化
CREATE INDEX idx_notifications_user_id ON notifications(user_id);
CREATE INDEX idx_notifications_is_read ON notifications(is_read);
```

### 缓存策略

- **客户端缓存**: React Query / SWR
- **CDN 缓存**: 静态资源
- **数据库缓存**: Supabase 内置缓存

## 🚀 扩展性设计

### 水平扩展

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Web App 1  │     │  Web App 2  │     │  Web App 3  │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                   │                   │
       └───────────────────┴───────────────────┘
                           │
                    ┌──────▼──────┐
                    │ Load Balancer│
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  Supabase   │
                    │  (自动扩展)  │
                    └─────────────┘
```

### 功能模块化

```
lib/api/
├── auth.ts          # 认证模块
├── venues.ts        # 场馆模块
├── bookings.ts      # 预订模块
├── teams.ts         # 组队模块
└── notifications.ts # 通知模块
```

## 📈 监控和日志

### 关键指标

- API 响应时间
- 数据库查询性能
- 用户活跃度
- 预订转化率
- 错误率

### 日志级别

```typescript
// 错误日志
console.error('Error creating booking:', error);

// 警告日志
console.warn('User session expired');

// 信息日志
console.info('Booking created successfully');

// 调试日志
console.debug('API request:', params);
```

## 🔄 实时功能

### Realtime 订阅

```typescript
// 订阅通知更新
const subscription = supabase
  .channel('notifications')
  .on('postgres_changes', {
    event: 'INSERT',
    schema: 'public',
    table: 'notifications'
  }, (payload) => {
    // 处理新通知
    handleNewNotification(payload.new);
  })
  .subscribe();
```

## 📱 移动端适配

### 响应式设计

```css
/* 移动端优先 */
.container {
  max-width: 100%;
  padding: 1rem;
}

/* 平板 */
@media (min-width: 768px) {
  .container {
    max-width: 768px;
  }
}

/* 桌面 */
@media (min-width: 1024px) {
  .container {
    max-width: 1024px;
  }
}
```

## 🔮 未来架构演进

### 微服务化

```
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│ 场馆服务     │  │ 预订服务     │  │ 支付服务     │
└─────────────┘  └─────────────┘  └─────────────┘
       │                │                │
       └────────────────┴────────────────┘
                        │
                  ┌─────▼─────┐
                  │ API Gateway│
                  └───────────┘
```

### 事件驱动架构

```
事件发布者          消息队列          事件订阅者
    │                 │                 │
预订创建 ────────▶ Event Bus ────────▶ 发送通知
    │                 │                 │
    │                 ├────────────────▶ 更新统计
    │                 │                 │
    │                 └────────────────▶ 记录日志
```

---

**版本**: 1.0.0  
**最后更新**: 2024-01-16
